name: Deploy STG

on:
  push:
    branches:
      - staging

jobs:
  deploy-user:
    name: Deploy frontend-user to STG
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: code/www-user/package-lock.json

      - name: Install & Build
        working-directory: code/www-user
        run: |
          npm install
          npm run build

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Sync to S3
        run: aws s3 sync code/www-user/dist/ s3://${{ secrets.S3_BUCKET_STG }}/user --delete

      - name: Invalidate CloudFront
        run: aws cloudfront create-invalidation --distribution-id ${{ secrets.CF_DISTRIBUTION_USER_STG }} --paths "/*"

  deploy-admin:
    name: Deploy frontend-admin to STG
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: code/www-admin/package-lock.json

      - name: Install & Build
        working-directory: code/www-admin
        run: |
          npm install
          npm run build

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Sync to S3
        run: aws s3 sync code/www-admin/dist/ s3://${{ secrets.S3_BUCKET_STG }}/admin --delete

      - name: Invalidate CloudFront
        run: aws cloudfront create-invalidation --distribution-id ${{ secrets.CF_DISTRIBUTION_ADMIN_STG }} --paths "/*"

  deploy-backend:
    name: Deploy backend to STG
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: code/api/lambda_hono/package-lock.json

      - name: Install & Build
        working-directory: code/api/lambda_hono
        run: |
          npm install
          npm run build

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Package & Deploy to Lambda
        working-directory: code/api/lambda_hono
        run: |
          zip -r function.zip dist/ node_modules/
          aws lambda update-function-code \
            --function-name ${{ secrets.LAMBDA_FUNCTION_STG }} \
            --zip-file fileb://function.zip
          aws lambda wait function-updated \
            --function-name ${{ secrets.LAMBDA_FUNCTION_STG }}
          aws lambda update-function-configuration \
            --function-name ${{ secrets.LAMBDA_FUNCTION_STG }} \
            --environment "Variables={DB_HOST=${{ secrets.DB_HOST_STG }},DB_PORT=${{ secrets.DB_PORT }},DB_USER=${{ secrets.DB_USER }},DB_PASSWORD=${{ secrets.DB_PASSWORD_STG }},DB_NAME=${{ secrets.DB_NAME_STG }},COGNITO_USER_POOL_ID=${{ secrets.COGNITO_USER_POOL_ID_STG }},COGNITO_CLIENT_ID=${{ secrets.COGNITO_CLIENT_ID_STG }}}"
